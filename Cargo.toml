[package]
name = "oh-my-jitter"
version = "0.1.0"
edition = "2024"
description = "A discrete-time simulator for comparing client retry/backoff strategies under contention"
license = "MIT"

[dependencies]
rand = "0.10.2"
clap = { version = "4.6.6", features = ["derive"] }

